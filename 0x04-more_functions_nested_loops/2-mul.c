#include "main.h"

/**
 * mul - Entry point
 * @a: parameter
 * @b: parameter
 * Description: 'Print a text using write'
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
