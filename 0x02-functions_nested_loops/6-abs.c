#include "main.h"
#include <stdlib.h>

/**
 * _abs - Entry point
 * @n: : parameter
 * Description: 'Print a text using write'
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	int a;

	a = abs(n);

	return (a);
}
