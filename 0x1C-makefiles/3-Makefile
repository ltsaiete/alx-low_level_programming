CC = gcc
SRC = main.c school.c
OBJ = $(SRC:.c=.o)
NAME = school
RM = fclean

all: $(NAME)

$(NAME): $(OBJ)
	${CC} $^ -o $@

%.o: %.c
	${CC} -c -o $@ $<

clean:
	rm -f *~ ${NAME}

oclean:
	rm -f ${OBJ}

fclean: clean oclean

re: all
	@touch ${SRC}
