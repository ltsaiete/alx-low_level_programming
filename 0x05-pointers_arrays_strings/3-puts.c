#include "main.h"
#include <stdio.h>

/**
 * _puts - Entry point
 * @str: parameter
 * Description: 'Print a text using write'
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
	puts(str);
}
