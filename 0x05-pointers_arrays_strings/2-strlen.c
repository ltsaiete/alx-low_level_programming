#include "main.h"

/**
 * _strlen - Entry point
 * @s: parameter
 * Description: 'Print a text using write'
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	int len = strlen(*s);

	return (len);
}
